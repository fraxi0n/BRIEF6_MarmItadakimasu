(function (factory) {
	typeof define === 'function' && define.amd ? define(factory) :
	factory();
})((function () { 'use strict';

	const message = "OK";

	// Je suis un commentaire
	console.log(message);

}));
